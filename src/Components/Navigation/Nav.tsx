import React from "react";
import styles from "./Nav.module.css";

const Nav: React.FC = () => {

    return( 
        <nav className = { styles.navContainer } >
        </nav>
    )
}

export default Nav;